export type contactType = {
    name: string,
    email: string,
    id:string
}

export type headingType={
    tag:string,
    text:string,
}

export type postType={
    title:string,
    body:string,
}