import Heading from "../components/Heading";
const Posts = () => {
    return (
        <div>
            <h1>Posts</h1>
            <p>Next.js 13 was recently released, learn more and see the upgrade guide. Version 13 also introduces beta
                features like the app directory that works alongside the pages directory (stable) for incremental
                adoption. You can continue using pages in Next.js 13, but if you want to try the new app features, see
                the new beta docs.</p>
        </div>
    );
};

export default Posts;